services:
  selfwishes:
    build: .
    ports:
      - "6767:6767"
    volumes:
      - selfwishes_data:/app/data
    environment:
      - DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  selfwishes_data:
